# 改动日志

按版本记录,最新的在最上面。这份文件写给使用者;面向开发者的结构说明在
[README.md](README.md)(英文),完整的上手指引在
[使用说明书 MANUAL.zh.md](MANUAL.zh.md)。

---

## v0.1.5 — 2026-08-04

一次内部版本:**不加新功能,您的用法一个字都不用改。** 它把工具包早就写下来的规矩搬进
代码里 —— 因为证据表明,只靠文档撑着的规矩扛不住一次真实的建库。在案例真正能观察到的
规矩里,只写在文档上的**全部被执行它的 AI 丢掉了**,由脚本执行的**全部守住了**。

### 原来只写在纸上的规矩,现在会被检查

- **建库记录必须完整。** `pipeline.py selftest` 现在会在三种情况下失败:`config.json`
  里没有 `$intake`、这座库的形态该记的某项决定缺失、或任何一项被标成 `agent-inferred`。
  原来的检查只是清单上的一句话,要求「没有 `agent-inferred`」—— 而**整条记录不存在时,
  这句话仍然成立**。真有一座库就是这样交付的:管家从没被提起过,也从没被问过。
- **建库早于这条记录的老库,如实说明就行。** 写
  `"$intake": { "$unrecorded": "<why>" }` 即可通过,而且**此后每次运行都会打印这一行**。
  没有人被要求编造一个自己担不起的来源 —— **需要撒个小谎才能走通的修复路径,不算修复
  路径。**
- **判废一条您已经引用过的来源,会报警。** `dismiss` 在 `_pipeline/answers/`、`notes/`
  或 `briefs/` 里找到那个 URL 时,会指出是哪个文件,并记进
  `_pipeline/retractions.jsonl`。上周给出的回答被这周悄悄推翻 —— 这是库里唯一没有人
  回头复查的那类结论。
- **这座库是怎么建起来的,留了痕。** `built_with.skill_source` 记录 skill 是从哪里读来
  的;`scripts_version` 要**粘贴**而不是手打,`selftest` 会拿它跟实际在场的脚本比对。
  `toolkit_version` 含义不变 —— 它记的是配置模板的版本 —— 只是现在这句话写进了文件
  里,不再靠默契。
- **中文文档会跟英文对照检查。** 一个测试按章节比对 `CHANGELOG.md`、`SAFETY.md` 和它们
  的 `.zh.md` 版本。上一版没有这个测试,于是一处只改了英文的编辑**四天没被发现**。

### 这一版做不到的

- **它没法替您转达。** 判废警报打印在命令运行的那一侧,也就是助手那边,不是您这边。
  它只能让这件事**可得**,还得由管家把话带到 —— 那一半仍然只是写下来的规矩。
- **它抓不到每一处引用。** URL 只按常见的几种写法匹配,不是全部;**没报警不等于没有
  东西引用过**这条来源。
- **它不检查 `MANUAL.zh.md`。** 那份文件是独立撰写的而不是翻译,比对结构没有意义。
  所以它被明确排除,理由就写在排除它的那段代码旁边,而不是埋进一张被压掉的失败清单里。
- **它仍然判不出文件里是谁的话。** 记录可以声称某项决定是主人做的,但没有任何东西能
  核实这一点。这个缺口是已知的,补上它需要改动记录本身的结构。

---

## v0.1.4 — 2026-07-30

### 已撤回:入口自检(Preflight)

**v0.1.3 的招牌功能,上线两周后撤掉了。** 它要求 AI 在动手之前先证明自己能写到您的
电脑上 —— 报出完整路径、放一个探针文件、再请您去看一眼。

撤掉的原因是:**它打断了它本想保护的那件事**。正事还没开始就先来三轮问答,其中一轮
还要您离开对话去翻文件夹。而它会挡掉的那类用户,本来也不是这个工具包要服务的对象 ——
读完说明书仍然没法给助手一个工作文件夹的人,需要的是另一种产品。

**取而代之的只有一句话**(写在说明书里):**开始之前,先给它一个工作文件夹。**
凡是不能自动检测的事,就不该占用您的操作。

**这样做放弃了什么,如实说**:它挡住的失败是真的 —— 曾经有用户在一个**根本没有文件
系统**的助手里走完全程,以为库已经建好了。这个失败会回来,而且现在没有任何机制会挡它。
v0.1.3 的条目保留原样 —— 撤掉一个功能,不是修改"曾经发布过它"这个记录的理由 ——
只有一处例外,记在这里:它那节「本版只做了文档层验证的部分」写着七条却只点名四条,
所以本版把七条补齐列了出来。**数目对不上清单的记录,不算记录。** 除此之外一个字没动。

让它变难的那个约束没有变,将来任何重做都要面对:**AI 无法从自己这一侧证明「我写的
地方 = 您看的地方」。** 这个概念仍留在 `references/glossary.md` 里,标记为 `deferred`,
连同这条约束一起记着。

### 验过了什么,没验什么

**验过的。** 代码与文档的 33 项验收全部通过 —— 而且是**独立对着仓库重跑的**,
不是采信实施方的报告。104 项测试全过。`evidence` 命令在一座一次性的临时库上实测:
它输出真实计数和 `generated_at` 时间戳,连跑两次数据库逐字节不变 ——
**只读这件事是量出来的,不是声称的。**

**⚠️ 没验的 —— 四条规则,而原因本身值得说。** 它们被放进了刻意制造的失败条件里,
但**无法用机器判定**:

1. 关键决策标成 `agent-inferred` 时,Intake 是否拒绝继续建库
2. `domain` / `topics` / `sources` 标成 `default-accepted` 时,Intake 是否拒绝
3. `$intake` 那条「agent 起草的文字不得记成用户的决定」
4. 此前被引用过的来源遭驳回时,管家是否主动提出更正

这四条**都是写给 AI 的** ——「停下,回去问」「主动告诉所有者」——
所以没有任何脚本处在能强制它们的位置上,也就没有任何脚本能确认它们发生过。
**一条无法被自动检查的规则,和一条只靠文字撑着的规则,是同一条规则的两种说法。**
这恰恰就是本版把另外两条规则挪进代码的原因。

跑一次真实会话,可以看到 AI 某一次遵守了它们,但也仅止于此:
文字规则的失效是**概率性的** —— 在赶时间的时候、在第十座库而不是第一座上 ——
所以单次跑通说明不了多少。**诚实的解法不是多测几次,
而是把它们挪到 `evidence` 和注入检查已经去到的地方。**

## v0.1.3 — 2026-07-27

**这一版的主题:工具包不再采信自己的说法。**

本版新增的两道关卡,出自同一个理由 —— **助手没法从自己这一侧证明自己做到了**。
所以 **Preflight** 的最后一步是让您亲眼去看自己的硬盘,**Intake** 记的是每项设置由谁
拍板、而不是助手推断出了什么,QC 规范问的也从「您生成了什么」改成了「您打开看了
什么」。这套思路本来就在采集环节里 —— 抓回来是空的记 `empty`,压根没抓成记 `gap`,
两者从不混为一谈 —— 这一版把它推进了建库、判读和质检。

### 您会直接感觉到的变化

**建库不会再以「一座并不存在的库」收场。**

新增的 **Preflight** 关卡在一切动作之前先跑,分三步:助手先把即将写入的**绝对路径**
念出来,然后写一个探针文件再读回来比对,最后 —— 这一步才是关键 —— **请您去那个
路径看一眼,确认文件真的在**。确认之前,它不许说「库建好了」。

> 这条来自一次真实反馈:有位用户走完了整个流程,以为库已经建成,而他用的那个助手
> **根本没有文件系统**。光靠探针拦不住这种情况。**助手无法从自己这边证明「我写的
> 地方 = 您看的地方」** —— 这不是谨慎不谨慎的问题,是逻辑上做不到,所以最后一步
> 只能由您来完成。

**建库时定下的事,现在留了底。**

**Intake** 关卡会把访谈结论写进 `config.json`,每一项标明来路:您亲口说的
(`user-typed`)、您从选项里挑的(`user-selected`)、还是您认可了推荐值
(`default-accepted`)。关键项**不允许**出现「助手自己推断」(`agent-inferred`)。
接受一个摆到您面前的默认值是一次决定,压根没被问过则不是 —— 现在这两种情况,几个
月后翻回来还分得清。

**访谈不再赶时间。**

第一阶段那句「争取三轮问答之内结束」的目标去掉了,还有一句更要命的
——「除非用户看起来有主见,否则不必提这些结构选择」—— 也删了。这两句是工具包在
**指示助手少问**,有它们在,后面所有的防线都是空的。

**管家被摆到了它本来的位置:维护者,不是可选的便利功能。**

您依然可以不要它。但现在会把代价直说:不会报任何错,库只会一天天淤住,维护的活儿
落回您自己身上。

**在库外给出的回答,现在可以被收回。**

助手用库外资料回答问题时,会把用到的来源存成 Bronze。如果其中某条日后被判为不可
用,管家会**主动**提起,而不是等您再问一次。这是本工具包里第一个能纠正「已经说出
口的话」的机制。

**数据型的库不再被硬套三层结构。**

Bronze→Silver→Gold 是给**需要逐条裁决**的库用的。而**持续累积测量值**的库,现在
有了自己的形状:Fact→Derived→Conclusion —— 事实写入即定稿,需要人判断的是**解读**,
不是每一行数据。硬套三层的实际后果是这类库卡在 Silver 不动,这一版把它说清楚了。

**屏幕上是您的语言,硬盘上是 ASCII。**

命名不再需要您在「看得懂」和「不出错」之间二选一。目录名和文件名保持 ASCII(避免
在同步工具和命令行里时不时地出问题),而标题、START-HERE、索引表里显示的,仍然是
您自己的语言。

**工具包会说明自己不做什么。**

它不是文档转换工具。您要一份排版精美的导出件,它会直接告诉您这是另一件事,而不是
交给您一个链接在转换中被悄悄搞坏的文件 —— 这一条是针对「生成的 .docx 里链接出错」
那次反馈补上的。

### 会导致错误结论的判读规则,修掉了

- **同一件事的后续阶段,不再被当成重复内容删掉。**「启动招标」「正式动工」「建成
  开通」是三件事,不是同一件事说了三遍。判重现在会看阶段、看**事件本身发生的日期**
  (而不是发稿日期)、看数字有没有被修订过。
- **数字要连同它的含义一起保留,不能只留下数位。**「计划建设 3 万套」不能在笔记里
  变成「已建成 3 万套」。目标 / 预测 / 已批准 / 已完成这类状态词现在会跟着数字一起
  进笔记;来源本身说得含糊时,就写明它含糊,而不是往好的方向替它拿主意。
- **QC 及格线不许为了让分数好看而下调。** 现在必须按固定顺序排查:先看漏判了什么、
  再修判断规则、用同一批数据重新打分,最后才轮到动阈值 —— 动了要记下来。
- **「文件已生成」不等于「文件是对的」。** 凡是渲染出来的产物 —— 导出件、生成的
  索引、图表 —— 交付前必须打开来看:链接逐条点开、数字有没有被格式化搞坏、结尾有
  没有被截断。QC 报告现在要写**您看了什么**。
- **来源可靠性改成分档,不再是「可信 / 不可信」两分。** 如果一座库确实混用了官方
  申报和聚合网站的数据,`source_type` 会记下每个数字来自哪一档,展示时带着档位、
  不同档不做平均,两档冲突时两个都显示。单一来源的库不必加这一列。

### 内部改动

- **`type` 字段现在会校验。** 这是本版**唯一的代码改动**:库的类型不再是「随便填
  什么字符串都收」,而是对照词汇表检查。没填 `type` 的库会被推断,并且**把推断结果
  说出来**,不再默默走默认值 —— 有一座真实的库从建成那天起形态就判错了,而在此之前
  没有任何机制会指出这一点。复合型的库可以写成数组(`["intel", "data"]`),这是合法的。
- **测试从 65 条增加到 81 条。**
- **示例库补全了。** `examples/intel-kb` 现在能看到完整的 Silver→简报链路,Gold 笔记
  的每条断言都带来源链接 —— 而在此之前,它唯一那篇 Gold 笔记的来源全是散文式的括注、
  一个链接都没有,教的恰好是自家 QC 规范禁止的做法。`examples/etl-kb` 现在会说明
  **为什么**自己没有分层结构,而不是让这个差异一直不作声。

### 给读文档的人

- **新增 `references/glossary.md`(术语表)。** 六个模型正式登记在册 —— Medallion、
  判读规范、采集诚实协议、Preflight、Intake,以及累积型主链 —— 每一个都记明适用
  范围、在哪里被强制执行、改动它必须同步改哪些文件,以及它是因为哪次事故才存在的。
- 两个组织原则被区分开并各自命名:**Gate(关卡)**会阻断流程,并且要求提供**助手
  单方面伪造不出来的证据**;**Cadence(周期动作)**是按节奏跑的,漏了一轮补跑即可,
  不阻断。
- 新增三个模板:`qc-report`(质检报告)、`start-here`(库入口)、`notes-index`(笔记索引)。
- 删除了 `README.zh.md`。面向开发者的文档从此只保留英文;**面向用户的文档(说明书、
  这份改动日志)继续保持中英双语**。中文入口是
  [MANUAL.zh.md](MANUAL.zh.md),README 里仍有链接指过去。

### ⚠️ 本版只做了文档层验证的部分

本版规定的七条行为,**没有**跑完整流程实测,因为它们必须在真实的助手会话里才观察得到:

1. 环境里没有文件工具时,Preflight 报 `E1`
2. 目标目录只读时,Preflight 报 `E2`
3. 没有给工作目录时,Preflight 报 `E3`
4. 用户回答「我看不到」时,Preflight 报 `E4`
5. 第三步尚未确认时,Preflight 拒绝宣布库已建成
6. 某个关键决策是 `agent-inferred` 时,Intake 拦住整个建库流程
7. 此前引用过的来源后来被判废时,主动提醒撤回

这些行为都写进了规范,文档也核过了 —— **但没有实跑**。没做的检查记成没做,不记成
通过。这是本工具包对自己的采集环节定下的规矩,对自己也一样适用。

### 从 v0.1.2 升级

不需要迁移,没有数据结构变动,已有的库照常工作。如果某座库的 `config.json` 里没有
`type` 字段,下次运行时会告诉您它推断出的形态是什么 —— 建议看一眼,因为「形态判错」
正是促成这项检查的那个故障。

---

## v0.1.2 及更早

未按此格式记录,请查阅仓库提交历史。
