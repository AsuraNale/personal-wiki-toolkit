# AGENTS.md

## Skills

This repo's entire workflow is defined in `SKILL.md` at the repo root
(single source of truth — do not duplicate its content here).

- When the task involves building, organizing, importing, or growing a
  personal knowledge base / wiki / second brain / note system: **read
  `SKILL.md` fully first** and follow it exactly.
- If `SKILL.md` and this file ever conflict, `SKILL.md` wins.
- Supporting material: `setup/` (guided flows), `references/` (methodology,
  read on demand per the file map in SKILL.md), `scripts/` (deterministic
  helpers — run them, don't reimplement them).

Codex-native install (optional, enables auto-triggering): copy or link this
repo folder into `~/.agents/skills/personal-wiki-toolkit/`.
